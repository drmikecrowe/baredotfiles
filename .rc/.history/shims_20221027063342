_command_exists() {
    type "$1" &> /dev/null
}

if [[ -z $_shims_sourced ]]; then

    if
    DIR="$HOME/.dotbare"
    if [[ -d "$DIR" ]]; then
        [[ ":$PATH:" != *":/$DIR:"* ]] && source $DIR/dotbare.plugin.zsh
    fi

fi

export _shims_sourced=1
