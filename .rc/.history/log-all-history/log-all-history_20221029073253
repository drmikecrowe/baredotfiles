if [[ "$(id -u)" -ne 0 ]]; then


    # my helper functions to search

    # Search logs from this folder and below only
    hdgrep() {
        WHERE="$LAH_FOLDER/$(pwd)"
        grep -Rh $@ $WHERE
    }

    # search all logs
    hgrep() {
        WHERE="$LAH_FOLDER"
        grep -Rh $@ $WHERE
    }
fi
